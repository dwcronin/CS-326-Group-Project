<section id="attendee-list-panel" class="mt-8">
  <h2 class="text-lg font-medium text-gray-900 mb-4">Attendees</h2>

  <% if (attendees.length === 0) { %>
    <div class="rounded-md border border-gray-200 bg-white px-4 py-6 text-sm text-gray-600">
      No active attendees yet.
    </div>
  <% } %>
</section>