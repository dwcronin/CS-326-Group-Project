<div class="max-w-4xl mx-auto space-y-6">
  <div class="rounded-2xl bg-white border border-slate-200 shadow-sm p-6 md:p-8">
    <div class="flex flex-col md:flex-row md:items-start md:justify-between gap-4">
      <div>
        <div class="flex items-center gap-2 flex-wrap mb-3">
          <span class="px-3 py-1 rounded-full text-xs font-semibold uppercase tracking-wide bg-blue-100 text-blue-700">
            <%= event.category %>
          </span>

          <span class="px-3 py-1 rounded-full text-xs font-semibold uppercase tracking-wide bg-slate-100 text-slate-700">
            <%= event.status %>
          </span>
        </div>

        <h1 class="text-3xl md:text-4xl font-bold text-slate-900">
          <%= event.title %>
        </h1>

        <p class="mt-3 text-slate-600 leading-relaxed">
          <%= event.description %>
        </p>
      </div>
    </div>

    <div class="mt-8 grid grid-cols-1 md:grid-cols-2 gap-4 text-sm">
      <div class="rounded-xl bg-slate-50 border border-slate-200 p-4">
        <p class="text-slate-500 font-medium">Location</p>
        <p class="text-slate-900 font-semibold mt-1"><%= event.location %></p>
      </div>

      <div class="rounded-xl bg-slate-50 border border-slate-200 p-4">
        <p class="text-slate-500 font-medium">Capacity</p>
        <% if (event.capacity) { %>
          <p class="text-slate-900 font-semibold mt-1"><%= event.capacity %></p>
        <% } else { %>
          <p class="text-slate-900 font-semibold mt-1">No limit</p>
        <% } %>
      </div>

      <div class="rounded-xl bg-slate-50 border border-slate-200 p-4">
        <p class="text-slate-500 font-medium">Starts</p>
        <p class="text-slate-900 font-semibold mt-1"><%= event.startDatetime.toLocaleString() %></p>
      </div>

      <div class="rounded-xl bg-slate-50 border border-slate-200 p-4">
        <p class="text-slate-500 font-medium">Ends</p>
        <p class="text-slate-900 font-semibold mt-1"><%= event.endDatetime.toLocaleString() %></p>
      </div>
    </div>

    <div
      class="mt-8 flex flex-col sm:flex-row gap-3 sm:items-center"
      x-data="{ confirming: false }"
    >
      <a
        href="/events"
        class="inline-flex justify-center px-4 py-2.5 rounded-lg bg-white border border-slate-200 text-slate-700 font-semibold hover:bg-slate-50 no-underline"
      >
        Back to Events
      </a>

      <% if (session.authenticatedUser && (session.authenticatedUser.role === "admin" || session.authenticatedUser.role === "staff")) { %>
        <a
          href="/events/<%= event.id %>/edit"
          class="inline-flex justify-center px-4 py-2.5 rounded-lg bg-blue-600 text-white font-semibold hover:bg-blue-700 no-underline"
        >
          Edit Event
        </a>
      <% } %>

      <% if (session.authenticatedUser && session.authenticatedUser.role === "user" && event.status === "published") { %>
        <form method="post" action="/events/<%= event.id %>/rsvp" x-ref="rsvpForm">
          <button
            type="button"
            x-on:click="confirming = true"
            class="w-full sm:w-auto px-4 py-2.5 rounded-lg bg-green-600 text-white font-semibold hover:bg-green-700"
          >
            RSVP
          </button>

          <div
            x-show="confirming"
            x-cloak
            class="mt-3 rounded-xl border border-green-200 bg-green-50 p-4"
          >
            <p class="text-sm text-green-800 font-medium">
              Confirm RSVP for this event?
            </p>

            <div class="mt-3 flex gap-2">
              <button
                type="submit"
                class="px-3 py-2 rounded-lg bg-green-600 text-white text-sm font-semibold hover:bg-green-700"
              >
                Yes, RSVP
              </button>

              <button
                type="button"
                x-on:click="confirming = false"
                class="px-3 py-2 rounded-lg bg-white border border-green-200 text-green-700 text-sm font-semibold hover:bg-green-100"
              >
                Cancel
              </button>
            </div>
          </div>
        </form>
      <% } %>
    </div>
  </div>
</div>